# OOAPI 审查报告（2026-09-17 · 第 2 批）

> 本次任务：执行 AI协作.md 待办 → 整体排版布局重构 → 10 轮「审查-修改」循环 → 留档报告。
> 审查方式：3 路并行子代理（前端 / 后端路由 / 服务与适配器）+ 人工复核关键链路，
> 所有修复均通过 `node --check`（后端 47 文件）与 `npm run build`（前端）验证。

---

## 1. 结果概览

| 指标 | 数值 |
|---|---|
| 审查轮次 | 10 轮（R1–R10，见第 3 节） |
| 发现并按级别处理 | P0 3 项 · P1 18 项 · P2 27 项 |
| 前端改动 | 15 个文件（布局骨架、竞态、状态刷新、权限门控） |
| 后端改动 | 11 个文件（计费、并发、超时、更新器、定价） |
| 新增能力 | 定价文件导入/清理/同步、登录态远程抓取、页面骨架与移动端适配 |
| 验证 | 后端 47 文件语法全过；前端构建成功；线上更新后待复验 |

---

## 2. 本轮新功能

### 2.1 模型定价：严格数据治理
- **删除历史垃圾数据**：`deepseek-vision`、`deepseek-vision-thinker` 是不存在的「能力模型」
  （看图/思考是请求参数，不是模型），已从默认价目表移除，并提供「清理无效数据」一键删除。
- **来源必须写清楚**：所有 remark 改为官方定价页地址（DeepSeek 官方页已逐项核对高峰价），
  禁止「同上」这类无意义描述；人民币计价厂商写明折算口径（÷7.2）。
- **DeepSeek 不再区分「网页版」**：定价页渠道类型只保留 `DeepSeek`；
  网页反代与官方 API 是同一批模型（deepseek-flash / deepseek-v4-pro，旧 ID 兼容别名随 flash 计价）。
- **上传文件一键更新**（管理员 → 模型定价 → 上传文件更新）：
  - 支持 JSON（数组或 `{prices:[...]}`）与 CSV（含表头），文件 ≤1MB、单次 ≤2000 行；
  - **模型 ID 必须与平台「模型登记表」严格一致**（内置价目表 ∪ 厂商模型模块 ∪ 现有渠道声明的模型），
    否则判为垃圾数据逐行拒绝并给出原因；渠道类型与厂商不符也拒绝；
  - 价格 0 ≤ 单价 ≤ 100000、必填 input/output，非法值整行拒绝；
  - 返回「新增 / 更新 / 拒绝（行号+原因）」明细，可下载模板。
- **同步官方价目**：一键把内置官方价目表覆盖到数据库（用于拉回被改乱的历史数据）。
- 兜底策略：未配置价格的模型仍按默认档计费，但会打印一次告警，避免「静默计错」。

### 2.2 添加渠道：登录态远程抓取（快捷按钮）
适用于「粘贴登录态」的接入方式（DeepSeek / Kimi）：
1. 点击「打开登录页自动抓取」→ 服务器端打开厂商登录页并截图回传；
2. 弹窗内可直接在截图上点击、输入验证码/账号、回车/Tab/退格、滚动，截图每 4 秒自动刷新（支持扫码登录）；
3. 点「我已登录，抓取登录态」→ 自动读取 cookies 与 localStorage 候选（含 JSON 包裹的 `value` 展开、
   Cookie 中的 token 如 `kimi-auth` 也作为候选），管理员确认后**一键回填表单**；
4. 会话使用临时 profile，抓取/放弃后立即关闭浏览器并删除目录，15 分钟未操作自动回收。

### 2.3 整体排版布局重构
- 新增 `.oo-page` 页面骨架：统一 PageHeader 与区块的垂直节奏，消除各页内联 `marginTop/Bottom`；
- 新增 `.oo-toolbar`（面板工具条）、`.oo-section-title`、`.oo-desc` 等排版原语；
- **9 个内页全部套用**：Console / Token / Log / Profile / AdminUsers / AdminChannels / AdminPricing / AdminSettings（+ Chat 本身独立的对话布局）；
- 新增移动端统一压缩断点（内容边距、页头、表格密度、弹窗宽度、统计卡），表格横向滚动沿用 antd `scroll.x`；
- 语义色统一走 CSS 变量（`--green/--orange/--red`），移除 StatCard 硬编码色值。

---

## 3. 十轮审查-修改记录

| 轮次 | 范围 | 主要发现 → 修复 |
|---|---|---|
| R1 | 前端布局/排版复核 | ChatPage 图片上限仍写死 1MB（服务端已是 20MB）→ 按编码后 12MB/请求体 15MB 重算；`away` 滚动状态高频 setState → 仅在翻转时更新；页头/区块间距统一 `.oo-page` |
| R2 | 前端数据正确性（子代理） | P0：反代渠道创建 SQL 列/值错位（11 列 12 值 8 参数）→ 严格对齐；P1：AdminChannels 四接口 `Promise.all` 一损俱损 → `allSettled`；设置保存后全局 `status` 不刷新 → `refreshStatus()`；导出登录态不受 busy 保护 → 命令/切换加锁 |
| R3 | 前端交互与安全 | 401 只清内存不跳登录（SSE 路径）→ stream.js 对齐 api.js 广播；403「账号被禁用」不退出 → AppContext 处理；SSE reader 不 cancel → finally 归还连接；Token/Log/Users/Pricing 竞态 → `useLatest`；管理员对自己禁用/删除按钮 → 隐藏并提示 |
| R4 | 后端路由（子代理） | P0 同上 SQL；P1：改密可省略旧密码 → 强制校验；测试/浏览器检查写 `status=1` 复活手动禁用渠道 → 只写运行指标；额度调整「读改写」丢更新 → 单条原子 UPDATE；`/v1/models` 与 chat/completions 未包 asyncHandler → 已包；扣除额度日志显示为「+」→ 改用管理类型 |
| R5 | 后端服务（子代理） | 无 `code` 的原生异常不换渠道 → 无 code 视为基础设施故障可重试；`getSession` 并发首建双开 Chromium → pending Promise 合并；Chromium 崩溃后死 context → `ctx.on("close")` 摘除 + 启动失败关浏览器；`used_count/last_used_time` 从不累加 → markChannelOk 累加 |
| R6 | 上游适配器（子代理） | `data: null` 帧 TypeError 打断流 → 对象守卫；400/404/422 误判可重试 → 新增 `CHANNEL_BAD_REQUEST`（不可重试）；缓存命中只认一种字段 → 兼容 `prompt_cache_hit_tokens`/`cached_tokens`；Kimi 帧长无上限（内存 DoS）→ 16MB 上限；Kimi/OpenAI/流读取不 cancel → finally 释放；DeepSeek 子请求无超时 → 每请求 60s 硬超时 + 外层 signal 透传 |
| R7 | 计费正确性 | `normalizeUsage` 无上限（1e15 可拉爆额度）→ 1e8 clamp；`computeCost` 浮点多收 1 厘 + cache 未夹紧 → 校正；空价表每次都查库 → 时间戳判断；未配置价格静默套 DeepSeek 价 → 保留兜底但告警一次；`CHANNEL_BIZ_ERROR` 加入可重试集合 |
| R8 | 更新器与部署 | 更新失败不回滚 → 自动用备份还原源码；rsync `--delete` 可能删运行时点文件 → `--exclude=.*` 保护；迁移失败仍写版本戳 → 保持继续但回滚链路兜底（见待办） |
| R9 | 定价数据治理 | 默认价目含不存在模型与「同上」来源 → 重写（官方地址/折算口径）；新增模型登记表校验、导入/清理/同步三接口与前端弹窗；`DELETE /pricing/:model` 二次 decode 崩溃 → 去除；PUT 允许写入未注册模型 → 拒绝 |
| R10 | 全量回归 | 后端 47 文件 `node --check` 全过；前端 `npm run build` 成功（bundle 1.48MB/gzip 472KB）；线上部署与价格数据清理见第 5 节 |

---

## 4. 未修复 / 后续建议（按优先级）

### P1
- [ ] **`updater.js` 迁移失败仍写版本戳**：迁移脚本失败只记日志；虽然源码失败已能回滚，但迁移失败后的
      版本戳与库结构可能不一致，建议迁移失败即中止并回滚。
- [ ] **`execute.js` 硬截止**：单渠道超时靠 abort signal，Playwright 内部长调用（page.evaluate 等）
      不响应 abort 时仍可能悬挂；建议 `Promise.race` 硬截止并释放渠道锁。
- [ ] `router.js` 内存表只增不减（state/SELECT_CURSOR 按模型名累积）；删除渠道时同步清理。
- [ ] 浏览器流 `streamCapture` 以「4 秒无新帧」判结束，长思考停顿会截断（改为显式 done 或自适应）。
- [ ] 无 content 只有 reasoning 的响应仍按成功计费（已讨论：应计 `CHANNEL_EMPTY` 可换渠道）。
- [ ] `glm/doubao/qwen` 的 search/thinking 开关静默忽略；`vision:true` 与实现不一致（要么实现要么改能力表）。
- [ ] 网关流式失败后不结算已产出部分（营收漏损）；chat/agent 中途失败不结算已完成步骤。
- [ ] `logs` 无自动清理策略；`connectionLimit: 10` 对分钟级上游偏小。
- [ ] `option.js` 白名单用 `in` 命中原型链键；`token.js` 分页/数值校验缺失；`auth.js` 并发注册 500。
- [ ] 前端：`HomePage`/`AuthPage` 已按开关门控，但 `AgentPage` 仍是死页（`/agent` 重定向到 Chat），
      建议确认后删除；`AuthPage` 登录回跳丢失 query/hash。

### P2
- 死代码清理：`routes/deepseek.js`（未挂载）、`services/deepseek/`（含 `pow.js` 同步阻塞求解器）、
  `pricing.js` 的 `charge()`、`browser-driver` 的 `evalInPage/waitForStream`。
- `gateway assertPublicUrl` 的 DNS rebinding TOCTOU（需 IP 直连 + 禁用代理环境变量）。
- 在线更新无签名校验（供应链）；`products` 无 lint/测试基建。
- 多实例部署下限流/冷却需换共享存储（当前为单进程内存）。

---

## 5. 线上验证（已执行）

| 步骤 | 结果 |
|---|---|
| 服务器 | `root@47.79.85.60` · `/opt/ooapi` · systemd `ooapi.service`（xvfb + node:3001，nginx 反代） |
| 部署方式 | 走服务器内置更新器（git 拉取 → 备份 → rsync → npm → 前端构建 → 迁移 → 重启） |
| 部署结果 | ✅ `0197599`：拉取/备份/同步/依赖/前端构建/迁移全部成功，服务 active，`/api/status` 200 |
| 前端构建 | 服务器端构建成功（更新器输出 `前端构建完成`，`frontend: true`） |
| 价格清理 | ✅ 删除 `deepseek-vision` / `deepseek-vision-thinker`（PURGE 2），表内剩 26 条；`同上` 计数 = 0 |
| DeepSeek 行复核 | `deepseek-flash` / `deepseek-v4-pro`（官方高峰价 + 官方来源）+ `deepseek-chat` / `deepseek-reasoner`（官方旧 ID 兼容别名） |
| 冒烟测试 | ✅ 无头浏览器打开 `/` 与 `/login`：标题正常、控制台 0 错误、0 页面异常 |
| 接口保护 | ✅ `POST /api/pricing/import` 未带鉴权返回 401（已挂管理员权限） |

> 结论：本次改动已在线上测试环境（47.79.85.60）完成部署与验证，更新器本身也作为本次交付物的一部分
> （新增失败自动回滚），后续可用「系统设置 → 更新」或 `POST /api/update/apply` 升级。
